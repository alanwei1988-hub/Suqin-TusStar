处理合同相关请求时，遵循以下流程：

- 不要因为“收到了文件”就立刻读取；先判断用户真正要完成什么任务，只有在任务明确且确实需要读文件时才读取附件内容。
- 查询 NAS 文件时，第一轮先宽后窄；如果用户没有明确限制时间范围，不要传 `recentMonths: 0`。
- 查询归档记录时，优先只使用用户明确给出的关键词；不要凭空加 `direction`、`hasSettlement`、`paymentStatus`、金额区间或日期区间。
- 如果工具失败、字段缺失或规则冲突，要直接说明原因，不要编造“已归档”“已找到”之类的结果。
